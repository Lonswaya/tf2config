bind "mouse2" "+attack2"
bind 1             slot1
bind 2             slot2
bind 3             slot3
bind mwheelup      invprev
bind mwheeldown    invnext
bind q             lastinv
bind mouse1        +attack
bind w             +forward
bind s             +back
bind a             +moveleft
bind d             +moveright 
r_drawviewmodel    1

exec diceroll